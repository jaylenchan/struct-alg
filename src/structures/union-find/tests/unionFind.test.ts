import { describe, it, expect } from 'bun:test';

describe('UnionFind', () => {
  it('Quick Find Test', () => {
    expect(1).toBe(1);
  });

  it('Quick Union Test', () => {
    expect(1).toBe;
  });
});
