// eslint-disable-next-line import/no-unresolved
import {describe, it, expect} from 'bun:test'
import MatrixGraph from 'tsalg/structures/graph/matrixGraph-impl'


describe('DepthFirstSearch', () => {
  it('', () => {
    const g = new MatrixGraph([])
    // console.log(g)
    expect(1, ).toBe(g)
  })
})
